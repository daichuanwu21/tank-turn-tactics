export const COLUMNS = "abcdefghijklmnopqrstuvwxyz".split(""); // used to generate labels/keys for squares, array length determines rendered board size
export const ROWS = 10; // defines number of rows on board
export const SQUARE_SIZE = 120; // defines width and height of each square, impacts rendered board size
