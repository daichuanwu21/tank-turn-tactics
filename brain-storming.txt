tank object:
{
  uuid: string;
  position: string; // {column}_{row number} e.g. a_1 or g_9, NOTE: row starts from 0 internally!
  range: number;
  health: number;
  ap?: number;
}

redux initial state: Tank[]


actions list:
INITIAL_SYNC
ADD_TANK
REMOVE_TANK
UPDATE_TANK (does not include AP, only current player AP is sent)
